{
  "name": "infra-stacks",
  "version": "0.0.0",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "license": "MIT",
  "scripts": {
    "dev": "sst dev --stage=default",
    "deploy:staging": "sst deploy --stage=staging",
    "remove:dev": "sst remove --stage=default",
    "remove:staging": "sst remove --stage=staging",
    "clean": "rm -rf .turbo && rm -rf node_modules"
  },
  "devDependencies": {
    "@types/node": "^18.16.16",
    "aws-cdk-lib": "2.84.0",
    "constructs": "10.1.156",
    "sst": "^2.19.2",
    "typescript": "^5.1.6"
  }
}
